# PromptInvoice

Современное мобильное приложение для создания и управления инвойсами (счетами) с поддержкой AI-заполнения.

## 📱 Описание

PromptInvoice — это полнофункциональное приложение для создания инвойсов, разработанное на React Native с использованием Expo. Приложение позволяет быстро создавать профессиональные счета, автоматически заполнять их с помощью AI, генерировать PDF-документы и экспортировать все инвойсы одним файлом.

## ✨ Основные возможности

### 🎯 Основной функционал
- **Создание и редактирование инвойсов** — полный CRUD функционал для управления счетами
- **Автоматическая нумерация** — номера инвойсов генерируются автоматически (INV-001, INV-002, ...)
- **Валидация уникальности** — система не позволяет использовать уже существующие номера инвойсов
- **Календарь выбора дат** — удобный компонент для выбора даты и срока оплаты
- **Расчет итогов** — автоматический расчет подытога, налога, скидки и итоговой суммы

### 🤖 AI-интеграция
- **Умное заполнение форм** — опишите счет словами, и AI автоматически заполнит форму
- **Гибкий парсинг** — AI извлекает любые данные из текста (клиент, позиции, налоги, скидки, заметки)
- **Поддержка Hugging Face** — использует модель Meta-Llama-3-8B-Instruct для обработки запросов

### 📄 PDF-генерация
- **Профессиональные PDF** — красиво оформленные инвойсы в формате PDF
- **Экспорт одного инвойса** — генерация и отправка отдельного счета
- **Экспорт всех инвойсов** — создание единого PDF-файла со всеми инвойсами и сводной статистикой
- **Системный шаринг** — интеграция с системным диалогом отправки файлов

### 🎨 Дизайн
- **Темная тема** — современный темный интерфейс с акцентными цветами
- **Анимированные модальные окна** — плавные переходы и красивые эффекты
- **Адаптивный UI** — удобный интерфейс для мобильных устройств
- **Иконки и визуальные элементы** — использование @expo/vector-icons для единообразия

## 🛠 Технологический стек

### Основные технологии
- **Framework:** Expo (~54.0.31)
- **Router:** Expo Router (~6.0.21) — файловая маршрутизация
- **State Management:** Zustand (^5.0.10)
- **UI:** React Native Standard Components
- **Icons:** @expo/vector-icons (^15.0.3)

### Специализированные библиотеки
- **PDF:** expo-print (~15.0.8), expo-sharing (~14.0.8)
- **Хранение данных:** @react-native-async-storage/async-storage (^2.2.0)
- **AI:** @huggingface/inference (^4.13.9)
- **UI компоненты:** @react-native-community/slider (^5.1.2)

### Языки и инструменты
- **TypeScript** (^5.9.2)
- **React** (19.1.0)
- **React Native** (0.81.5)

## 🚀 Установка и запуск

### Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** (версия 18 или выше)
- **npm** или **yarn** (менеджер пакетов)
- **Expo CLI** (устанавливается глобально через npm)
- **Git** (для клонирования репозитория)

Для разработки на мобильных устройствах:

- **iOS:** macOS с установленным Xcode (для симулятора) или физическое устройство с приложением Expo Go
- **Android:** Android Studio с эмулятором или физическое устройство с приложением Expo Go

### Установка зависимостей

1. Клонируйте репозиторий (если еще не сделали):
```bash
git clone <repository-url>
cd PromptInvoice
```

2. Установите зависимости проекта:
```bash
npm install
```

или с использованием yarn:
```bash
yarn install
```

### Запуск в режиме разработки

После установки зависимостей вы можете запустить приложение:

#### Универсальный запуск
```bash
npm start
```
или
```bash
yarn start
```

Это откроет Expo Dev Tools в браузере, где вы сможете выбрать платформу для запуска.

#### Запуск для конкретной платформы

**iOS (только на macOS):**
```bash
npm run ios
```
или
```bash
yarn ios
```

**Android:**
```bash
npm run android
```
или
```bash
yarn android
```

**Web:**
```bash
npm run web
```
или
```bash
yarn web
```

### Настройка окружения

Для работы AI-функций необходимо настроить API ключ Hugging Face:

1. Получите API ключ на [Hugging Face](https://huggingface.co/settings/tokens)
2. Создайте файл `.env` в корне проекта (если его нет)
3. Добавьте ваш API ключ:
```
EXPO_PUBLIC_HF_TOKEN=your_api_key_here
```

**Примечание:** Файл `.env` должен быть добавлен в `.gitignore` для безопасности.

### Билд для production

#### Android (APK/AAB)

1. Установите EAS CLI (если еще не установлен):
```bash
npm install -g eas-cli
```

2. Войдите в аккаунт Expo:
```bash
eas login
```

3. Настройте проект:
```bash
eas build:configure
```

4. Соберите APK:
```bash
eas build --platform android --profile preview
```

5. Или соберите AAB для Google Play:
```bash
eas build --platform android --profile production
```

#### iOS (IPA)

1. Убедитесь, что у вас есть Apple Developer аккаунт
2. Настройте проект:
```bash
eas build:configure
```

3. Соберите приложение:
```bash
eas build --platform ios --profile production
```

#### Web

Для статического билда веб-версии:
```bash
npx expo export:web
```

Собранные файлы будут в папке `web-build/`.

### Дополнительные команды

**Линтинг кода:**
```bash
npm run lint
```

**Сброс проекта (очистка кэша):**
```bash
npm run reset-project
```

## 📁 Структура проекта

```
PromptInvoice/
├── app/                    # Файловая маршрутизация (Expo Router)
│   ├── _layout.tsx        # Корневой layout
│   ├── index.tsx          # Главный экран со списком инвойсов
│   └── create.tsx         # Экран создания/редактирования инвойса
├── components/            # Переиспользуемые UI компоненты
│   ├── ConfirmModal.tsx   # Модальное окно подтверждения
│   ├── DatePicker.tsx     # Календарь для выбора даты
│   ├── InvoiceCard.tsx    # Карточка инвойса в списке
│   ├── ItemRow.tsx        # Строка позиции в инвойсе
│   └── Modal.tsx          # Базовое модальное окно
├── services/              # Бизнес-логика и внешние сервисы
│   ├── aiService.ts       # Интеграция с Hugging Face AI
│   └── pdfService.ts      # Генерация PDF документов
├── store/                 # Управление состоянием (Zustand)
│   └── useInvoiceStore.ts # Store для инвойсов
└── types/                 # TypeScript типы и интерфейсы
    └── invoice.ts         # Типы для инвойсов
```

### Создание нового инвойса

1. На главном экране нажмите кнопку **"+"** (создать)
2. Заполните форму:
   - **Client Name** — имя клиента (обязательно)
   - **Invoice Number** — номер инвойса (генерируется автоматически, можно изменить)
   - **Date** — дата инвойса (обязательно)
   - **Due Date** — срок оплаты (опционально, выбирается через календарь)
3. Добавьте позиции (items):
   - Нажмите **"Add"** для добавления новой позиции
   - Заполните описание, количество и цену
4. Настройте финансы:
   - **Tax** — ставка налога (0-100%)
   - **Discount** — скидка (0-100%)
5. При необходимости добавьте заметки
6. Нажмите **"Save"** для сохранения

### AI-заполнение формы

1. В разделе **"AI Fill"** введите описание счета текстом
   - Пример: `"Invoice for Apple Inc, 3 logo designs for $500 each, tax 20%"`
2. Нажмите **"Fill with AI"**
3. AI автоматически заполнит форму всеми найденными данными

### Редактирование инвойса

1. На главном экране нажмите на карточку инвойса
2. Внесите необходимые изменения
3. Нажмите **"Save"** для сохранения изменений
4. Или нажмите иконку корзины для удаления инвойса

### Генерация PDF

#### Отдельный инвойс
1. Откройте инвойс для редактирования
2. Нажмите кнопку **"Share PDF"** внизу формы
3. Выберите способ отправки из системного диалога

#### Все инвойсы
1. На главном экране нажмите зеленую кнопку с иконкой загрузки (рядом с кнопкой создания)
2. Будет создан PDF-файл со всеми инвойсами и сводной статистикой
3. Выберите способ отправки из системного диалога

## 🎨 Особенности дизайна

### Цветовая палитра
- **Основной фон:** `#0D0D1A` (глубокий темно-синий)
- **Карточки:** `#1A1A2E` (темно-фиолетовый)
- **Акценты:**
  - Фиолетовый `#6366F1` — основные действия
  - Зеленый `#10B981` — успех, итоги, экспорт
  - Оранжевый `#F59E0B` — AI функции
  - Красный `#EF4444` — ошибки, удаление
  - Розовый `#EC4899` — позиции
  - Желтый `#FBBF24` — заметки

### Компоненты
- **Модальные окна** — самописные с анимациями и эффектами свечения
- **Календарь** — кастомный компонент с навигацией по месяцам
- **Карточки инвойсов** — с акцентной линией и иконками
- **Слайдеры** — для настройки налога и скидки с визуальными индикаторами

## 🔧 Технические детали

### Управление состоянием
- Используется Zustand для глобального состояния
- Все инвойсы сохраняются в AsyncStorage для персистентности
- Автоматическая синхронизация при изменении данных

### Валидация
- Проверка обязательных полей
- Валидация форматов (даты, числа)
- Проверка уникальности номеров инвойсов
- Валидация элементов позиций

### Генерация номеров
- Автоматическая нумерация от INV-001
- Поиск максимального номера среди существующих инвойсов
- Возможность ручного редактирования с проверкой уникальности

### AI-обработка
- Использование модели Meta-Llama-3-8B-Instruct
- Гибкий парсинг любых данных из текста
- Обработка ошибок и валидация JSON ответов
- Поддержка частичного заполнения (только найденные поля)

```

## 🐛 Известные ограничения

- AI требует активное интернет-соединение
- PDF генерация работает только на реальных устройствах (не в веб-версии)
- Данные хранятся локально (нет облачной синхронизации)

---